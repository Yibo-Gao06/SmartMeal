<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.smartmeal</groupId>
    <artifactId>smartmeal-platform</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>smartmeal-platform</name>
    <description>智能膳食规划与生鲜导购平台 - AI 驱动的膳食规划 + 生鲜电商交易闭环</description>

    <modules>
        <module>smartmeal-common</module>
        <module>smartmeal-domain</module>
        <module>smartmeal-repository</module>
        <module>smartmeal-service</module>
        <module>smartmeal-ai</module>
        <module>smartmeal-job</module>
        <module>smartmeal-app-api</module>
        <module>smartmeal-admin-api</module>
        <module>smartmeal-deploy</module>
    </modules>

    <properties>
        <!-- Spring Boot 3 基线为 Java 17 LTS，勿用 21+ 以免 Lombok / MyBatis-Plus 反射踩坑 -->
        <java.version>17</java.version>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <spring-ai.version>1.1.8</spring-ai.version>
        <mybatis-plus.version>3.5.17</mybatis-plus.version>
        <sa-token.version>1.46.0</sa-token.version>
        <knife4j.version>4.5.0</knife4j.version>
        <!-- Knife4j 4.5.0 自带的 springdoc 版本偏旧，在 Boot 3.5 上会 NoSuchMethodError，必须显式抬到 2.9.x -->
        <springdoc.version>2.9.1</springdoc.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- ===== 第三方 BOM ===== -->
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ===== 三方组件 ===== -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.dev33</groupId>
                <artifactId>sa-token-spring-boot3-starter</artifactId>
                <version>${sa-token.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
                <exclusions>
                    <exclusion>
                        <groupId>org.springdoc</groupId>
                        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- ===== 内部模块 ===== -->
            <dependency>
                <groupId>com.smartmeal</groupId>
                <artifactId>smartmeal-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.smartmeal</groupId>
                <artifactId>smartmeal-domain</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.smartmeal</groupId>
                <artifactId>smartmeal-repository</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.smartmeal</groupId>
                <artifactId>smartmeal-service</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.smartmeal</groupId>
                <artifactId>smartmeal-ai</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.smartmeal</groupId>
                <artifactId>smartmeal-job</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <parameters>true</parameters>
                    <!-- 显式声明 Lombok 注解处理器：JDK 23+ 默认不再从 classpath 发现处理器，
                         写死可以保证换 JDK 也不会突然编不过 -->
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
